## Технології

### Frontend (Next.js)

- **Next.js 16** — React фреймворк
- **TypeScript** — типізація
- **shadcn/ui** — UI компоненти
- **Zustand** — управління станом
- **Zod** — валідація форм
- **Tailwind CSS** — стилізація

### Backend (NestJS)

- **NestJS** — Node.js фреймворк
- **PostgreSQL** — реляційна база даних
- **Prisma ORM** — сучасна ORM
- **JWT** — авторизація
- **bcrypt** — хешування паролів
- **class-validator** — валідація

## Що реалізовано

### Функціональність

- ✅ **Авторизація та реєстрація** — JWT токени, захищені routes
- ✅ **Профілі користувачів** — кожен пост прив'язаний до автора
- ✅ **Створення постів** — модальна форма з валідацією
- ✅ **Перегляд постів** — картки з автором, датою, тегами та лайками
- ✅ **Детальний перегляд** — модалка з контентом та коментарями
- ✅ **Справжні лайки** — зберігаються в БД, один користувач = один лайк
- ✅ **Коментарі** — додавання та перегляд коментарів
- ✅ **Пошук** — серверний пошук за заголовком, контентом, автором, тегами
- ✅ **Сортування** — за датою (новіші/старіші) та популярністю
- ✅ **CRUD операції** — створення, читання, оновлення, видалення (тільки автор)
- ✅ **REST API** — повноцінний бекенд з валідацією та авторизацією

### UI компоненти

- Всі UI елементи побудовані на базі **shadcn/ui**
- **Адаптивні модальні вікна** — Dialog на десктопі, Drawer на мобільних
- **Адаптивний дизайн** — оптимізовано для всіх розмірів екрану

### Структура проєкту

```
blog-test-task/
├── backend/                 # NestJS Backend
│   ├── prisma/
│   │   └── schema.prisma    # Prisma схема БД
│   ├── src/
│   │   ├── auth/            # Модуль авторизації (JWT)
│   │   ├── posts/           # Модуль постів (CRUD + лайки)
│   │   ├── comments/        # Модуль коментарів
│   │   ├── prisma/          # Prisma сервіс
│   │   ├── app.module.ts
│   │   └── main.ts
│   ├── .env                 # Environment змінні
│   └── package.json
│
├── src/                     # Next.js Frontend
│   ├── app/                 # Next.js App Router
│   │   ├── home/            # Головна сторінка (після логіну)
│   │   ├── login/           # Сторінка входу
│   │   ├── register/        # Сторінка реєстрації
│   │   └── layout.tsx
│   ├── components/          # React компоненти
│   │   ├── ui/              # shadcn/ui компоненти
│   │   ├── Header.tsx       # Шапка з кнопкою виходу
│   │   ├── LoginForm.tsx    # Форма входу
│   │   ├── RegisterForm.tsx # Форма реєстрації
│   │   ├── PostForm.tsx     # Форма створення поста
│   │   ├── PostCard.tsx     # Картка поста
│   │   ├── PostDetail.tsx   # Детальний перегляд
│   │   ├── CommentsSection.tsx
│   │   └── ...
│   ├── hooks/               # React хуки
│   │   ├── useAuth.ts       # Авторизація користувача
│   │   ├── usePosts.ts      # Робота з постами
│   │   ├── usePost.ts       # Один пост
│   │   └── useComments.ts   # Коментарі
│   ├── lib/
│   │   ├── api.ts           # API клієнт (REST)
│   │   ├── utils.ts         # Допоміжні функції
│   │   └── validations.ts   # Zod схеми
│   ├── providers/
│   │   └── AuthProvider.tsx # Провайдер авторизації
│   ├── store/
│   │   └── useBlogStore.ts  # Zustand store
│   └── types/
│       └── index.ts         # TypeScript типи
│
└── package.json             # Frontend залежності
```

## 🚀 Запуск проекту

### 1. Запуск Backend (NestJS)

```bash
cd backend

# Встановити залежності
npm install

# Створити .env файл
echo 'DATABASE_URL="postgresql://user:password@localhost:5432/blog_db?schema=public"
JWT_SECRET="your-super-secret-jwt-key"
JWT_EXPIRES_IN="7d"
PORT=3001
FRONTEND_URL="http://localhost:3000"' > .env

# Запустити PostgreSQL (Docker або локально)
docker run --name blog-postgres -e POSTGRES_PASSWORD=password -e POSTGRES_DB=blog_db -p 5432:5432 -d postgres

# Виконати міграцію БД
npx prisma migrate dev --name init

# Запустити сервер
npm run start:dev
```

Backend буде доступний на `http://localhost:3001/api`

### 2. Запуск Frontend (Next.js)

```bash
# Встановити залежності (з кореня проекту)
npm install

# Створити .env.local файл
echo 'NEXT_PUBLIC_API_URL=http://localhost:3001/api' > .env.local

# Запустити frontend
npm run dev
```

Frontend буде доступний на `http://localhost:3000`
